version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./chroma_db:/app/chroma_db
      - ./helix_srop.db:/app/helix_srop.db
    restart: always

networks:
  default:
    name: helix-srop-network
